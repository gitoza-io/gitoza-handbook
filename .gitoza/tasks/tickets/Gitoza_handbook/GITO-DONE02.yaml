---
title: Cancelled — duplicate auth toast
type: bug
status: cancelled
priority: low
tags:
  - handbook
  - auth
reporter: Gitoza Handbook
---

# Cancelled — duplicate auth toast

Example of a **cancelled** bug for status filter demos (distinct from `done`).

## Resolution note

Cancelled as duplicate of earlier auth work; kept in the handbook so filters have a `cancelled` row.

Filter tip: **Status** = `cancelled` should surface this ticket. Compare with [[GITO-DONE01]] (`done`) and open bugs like [[GITO-BUG001]].
